package control.max.ControleCobrancaCondominio.repositories;

import org.springframework.data.jpa.repository.JpaRepository;

import control.max.ControleCobrancaCondominio.models.Unidade;

public interface UnidadeRepo extends JpaRepository<Unidade, String> {

}
